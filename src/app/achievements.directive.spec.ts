import { AchievementsDirective } from './achievements.directive';

describe('AchievementsDirective', () => {
  it('should create an instance', () => {
    const directive = new AchievementsDirective();
    expect(directive).toBeTruthy();
  });
});
